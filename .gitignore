__pycache__/
*.py[cod]
*.pyo
*.tmp
*.temp
.DS_Store
Thumbs.db

# Runtime/output artifacts
output/
outputs/
tmp/
temp/
logs/
*.log

# Local editor state
.idea/
.vscode/

# Local-only site mapping for the LocalVault browser helper.  It may contain a
# URL, username, secret name, and captcha flag, but never a password.
localvault-credentials/local/accounts.json

# Legacy location only. Keep ignored permanently so credentials can never be committed.
/xf-product-case-registry/admin-upload-config.toml
